# 心灵日记 (Mind Journal) — 项目文档

## 项目概述

精神健康记录 PWA 应用，面向精神科患者和研究者。
- 患者每日记录情绪和症状，查看趋势
- 在知情同意前提下，匿名数据可供研究使用
- 支持紧急联系人、安心音乐、日记等辅助功能

---

## 核心创新点

### 1. 时段加权情绪算法

**问题**：传统方案每天只记录一个情绪分，忽略了日内波动。

**方案**：将一天分为 7 个生物学时段，用户可在任意时段记录情绪：

| 时段 | 时长 | 生物学权重 | 依据 |
|------|:---:|:---:|------|
| 00:00-06:00 深夜 | 6h | 0.6 | 褪黑素主导，睡眠期通常不记录 |
| 06:00-09:00 早晨 | 3h | 1.3 | 皮质醇觉醒反应(CAR)，情绪基线建立 |
| 09:00-12:00 上午 | 3h | 1.2 | 认知功能峰值，情绪最稳定 |
| 12:00-14:00 午间 | 2h | 0.8 | 餐后低潮，情绪波动不具代表性 |
| 14:00-18:00 下午 | 4h | 1.1 | 第二波精力，反映日常功能水平 |
| 18:00-22:00 傍晚 | 4h | 0.9 | 社会/家庭因素主导 |
| 22:00-24:00 深夜 | 2h | 0.7 | 睡前反刍，抑郁焦虑倾向放大 |

**加权公式**：
```
每日情绪分 = Σ(D_i × B_i × S_i) / Σ(D_i × B_i)
```
其中 D_i=时长权重, B_i=生物学权重, S_i=该时段记录分数。
只对已记录时段加权，不填补缺失数据。

> ⚠️ **当前状态**：权重系数为基于现有节律知识的建议值，待文献系统验证后调整。代码中权重作为可配置常量，修改不影响其余逻辑。

### 2. 情绪-症状分离架构

**问题**：传统方案混用"情绪"和"症状"，但临床上是两个维度。

**方案**：
- **情绪**（Emotions）：主观体���标签，不做评分加权，仅记录出现频率
- **症状**（Symptoms）：临床指标，每个症状有基础权重 + 三档严重度系数(×1/×2/×3)，计算每日症状总分

**症状权重分级**：
- 🔴 危机级 (8-10)：自杀想法、自伤行为、自杀尝试
- 🟠 严重级 (4-5)：躁狂、攻击欲望、敌意、自伤想法
- 🟡 中度级 (2)：失眠、嗜睡、暴食、注意力、社交回避
- 🟢 轻微级 (1)：手抖、头晕、食欲差

可计数症状（手抖、自伤等）额外记录频率，供临床参考但不影响评分。

### 3. 粒度化知情同意 + 审计追踪

- 用户可单独控制分享情绪数据 / 日记数据
- 每次变更记录到 `consent_logs` 表（完整审计轨迹）
- 撤回后旧记录标记 `revoked`，新数据不再同步
- 研究员只看到匿名 UUID，无法关联真实身份

### 4. 匿名 UUID + 无密码方案

- 用户标识使用 `crypto.randomUUID()` 生成
- 不收集手机号、邮箱、姓名
- 通过 JWT token 认证，token 长期有效
- 研究员使用用户名+密码+JWT 认证

### 5. 离线优先 PWA 架构

- 所有数据默认存 IndexedDB（Dexie.js）
- Service Worker 缓存静态资源，无网络也能使用
- 同步是用户主动操作，非自动上传
- 数据导出功能给用户完整数据主权

---

## 工作交接记录

### 对话 #1 — 2026-07-23 | 项目启动
**参与者**：用户、Claude

**讨论内容**：
- 确定 PWA + Python FastAPI 技术栈
- 确认移动端优先、离线优先架构
- 确定 MVP 范围为情绪记录 + 趋势图
- 数据存储方案：本地 IndexedDB 为主，可选同步到后端
- 知情同意粒度：情绪/日记分别控制
- 8 步实施计划制定

**产出**：
- 完整技术方案文档
- 项目目录结构
- [commit: initial setup]

### 对话 #2 — 2026-07-23 | 前端骨架 + 后端全部搭建
**参与者**：用户、Claude

**产出**：
- PWA 前端全部 5 个 Tab（记录/统计/日记/音乐/设置）
- Dexie.js 本地数据库封装
- Chart.js 趋势图 + 标签饼图
- 紧急联系人（一键拨打 tel:）
- 音乐上传 + 播放器（IndexedDB 存 Blob）
- 知情同意开关 + 数据导出 JSON
- Service Worker + manifest.json (PWA)
- FastAPI 后端 14 个 API 端点
- 数据库模型（User/MoodRecord/DiaryRecord/ConsentLog/Researcher）
- 所有 API 测试通过

**技术决策**：
- 使用 Dexie.js 而非原生 IndexedDB（API 更友好）
- 使用 Chart.js 而非 ECharts（体积小、对移动端友好）
- 单页应用（SPA）而非多页（导航体验更好）
- SQLite 作为开发数据库，可无缝切换 PostgreSQL

### 对话 #3 — 2026-07-23 | 时段记录 + 情绪症状分离 (v0.2)
**参与者**：用户、Claude

**产出**：
- 7 时段选择器（0-6/6-9/9-12/12-14/14-18/18-22/22-24）
- 加权情绪算法实现（时长权重 × 生物学权重，可配置）
- 情绪标签三分类（积极/中性/消极）与症状完全分离
- 15 种症状卡片，每个含三档严重度(轻/中/重 ×1/×2/×3) + 基础权重
- 症状分级：🔴危机(8-10) 🟠严重(4-5) 🟡中度(2) 🟢轻微(1)
- 可计数症状（手抖、自伤、自杀想法等）增加频率/次数追踪
- 统计页双图：加权情绪折线图 + 症状分柱状图
- 点击图表/历史记录弹出每日详情面板（时段+症状+频率）
- 后端新增 SymptomRecord 表、症状 API、研究症状查询
- DOCUMENTATION.md 项目诠释文档
- API 端点从 14 增至 17

**待定事项**：
- 生物学权重系数待文献验证
- 症状基础权重待临床验证
- 频率数据是否纳入评分公式待讨论

### 对话 #4 — 2026-07-23 | v0.3 每日曲线+症状搜索+单症状趋势
- 统计页新增「今日」Tab（7时段曲线），周/月图点击跳转详情
- 症状搜索框+分类筛选，symptom-config.js独立配置（39种/5分类）
- 单症状30天趋势图，手机局域网测试通过

### 对话 #5 — 2026-07-23 | v0.4 症状折叠+时段标记+色彩+排序+周摘要
- 症状列表默认折叠，已选症状彩色可移除徽章
- 时段选择器已记录标记checkmark，症状卡片边框变色(黄/橙/红)
- 症状智能排序(常用置顶)，滑杆5锚点，本周概况三格卡片
- 手机实测通过

### 对话 #6 — 2026-07-23 | v0.5 账号系统+内置环境音 [进行中]
- 前端登录/注册UI + 后端账号API(用户名+密码+JWT)
- Web Audio API内置环境音(白噪音/粉红/布朗/雨声/海洋/风铃)
- 紧急躯体化时一键播放，无需导入文件

---

## 技术债务 & 后续规划

- [ ] 生物学权重系数查阅文献验证
- [ ] 症状权重咨询精神科临床意见
- [ ] 后端研究员认证中间件（当前 MVP 简化）
- [ ] HTTPS 部署（PWA Service Worker 需要）
- [ ] TWA 打包上架 Google Play
- [ ] 多语言支持（当前仅中文）
- [ ] 单元测试
- [ ] 数据可视化优化（移动端小屏适配）

---

## 技术栈一览

| 层 | 技术 | 版本 |
|---|------|------|
| 前端框架 | 原生 HTML/CSS/JS | — |
| 图表 | Chart.js | 4.4.0 |
| 本地存储 | Dexie.js (IndexedDB) | 3.2.4 |
| 后端框架 | FastAPI (Python) | 0.139+ |
| 数据库 | SQLite (dev) / PostgreSQL (prod) | — |
| ORM | SQLAlchemy | 2.0+ |
| 认证 | python-jose (JWT) | 3.5.0 |
| PWA | Service Worker + Web App Manifest | — |
